# Function str()

emp <- list(empno = 11, name = "Alex", age = 32, dept = "sales")
str(emp)

# The Function str hands on is wrongly made on Fresco, you should create the code.r file using the command vim, (vim code.r)
# Code, and save and exit. Then click Continue